ver=1.2.2
src=https://musl.libc.org/releases/musl-$ver.tar.gz
files=(lib/libc.so)

configure() {
  $CONFIGURE $TOOLFLAGS --syslibdir=$LIB_PREFIX
}

postinstall() {
  #ln -sf $LIB/libc.so $BIN/ldd

  echo $LIB_PREFIX >> $ETC/ld-musl-$ARCH.path
}