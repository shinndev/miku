type=tar.xz
src=http://www.nano-editor.org/dist/v2.9/nano-2.9.1.$type
deps=(ncurses)

STATIC=yes

configure() {
  $CONFIGURE \
    --enable-color \
    --enable-nanorc \
    --enable-multibuffer \
    --enable-utf8 \
    --disable-nls \
    --disable-libmagic \
    --disable-wrapping
}