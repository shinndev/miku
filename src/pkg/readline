ver=7.0
type=tar.gz
src=https://ftp.gnu.org/gnu/readline/readline-$ver.$type
deps=(ncurses)
files=(libreadline.a)

configure() {
  CPPFLAGS="-D_GNU_SOURCE" \
  LDFLAGS="$LDFLAGS -Wl,-rpath-link=$LIB" \
  $CONFIGURE \
    --host=$HOST \
    --with-sysroot=$ROOT \
    --disable-nls
}