ver=7.0
type=tar.gz
src=https://ftp.gnu.org/gnu/readline/readline-$ver.$type
deps=(ncurses)
files=(libreadline.a)

build() {
  $CONFIGURE \
    --with-curses \
    --enable-shared \
    --enable-static

  $MAKE
  $MAKE_INSTALL
}