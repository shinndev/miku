group=libc
ver=1.2.2
src=https://musl.libc.org/releases/musl-$ver.tar.gz
files=(lib/libc.so)

configure() {
  $CONFIGURE \
    --syslibdir=$LIB_PREFIX
}

install() {
  $INSTALL

  ln -sf ../lib/libc.so $BIN/ldd

  echo $LIB_PREFIX >> $ROOT/etc/ld-musl-$ARCH.path
}