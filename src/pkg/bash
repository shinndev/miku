ver=5.1
type=tar.gz
src=https://ftp.gnu.org/gnu/bash/bash-$ver.$type
deps=(readline)
files=(bash)

STATIC=yes

build() {
  $CONFIGURE \
    --without-bash-malloc \
    --disable-nls \
    --with-installed-readline

  $MAKE
  $MAKE_INSTALL
}