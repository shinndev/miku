ver=5.1
type=tar.gz
src=https://ftp.gnu.org/gnu/bash/bash-$ver.$type
files=(bash)

STATIC=yes

configure() {
  $CONFIGURE \
    --host=$HOST \
    --without-bash-malloc \
    --disable-nls \
    --with-installed-readline
}