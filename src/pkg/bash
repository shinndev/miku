ver=5.1
type=tar.gz
src=https://ftp.gnu.org/gnu/bash/bash-$ver.$type
deps=(readline)

build() {
  ./configure \
    --prefix=$PREFIX \
    --with-curses \
    --without-bash-malloc \
    --disable-nls \
    --with-installed-readline

  make -j$JOBS
  make DESTDIR=$ROOT install
}